//shorthand Properties
//computed properties
const team = {
    [role]: person,
    [1 + 6 + 5]: "something"
}
//methods inside object
const math = {
    add: function(x,y){
        return x,y
    },
    multiply: (x, y) => x * y,
    division(x,y){
        return x/y
    } //shorthand
}

//THIS /this > reference to current execution scope or reference to global scope
//depends on innvocation context
//