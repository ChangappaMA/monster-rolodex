//Comparison Operators
//== equality -- != non equality > equality of values not type
//=== strict equality -- !== non equality > equality of values and type
// logical operators &&(AND) || (OR) ! (NOT)
//Operator Precedence 
// ! higher precendence than && and || 
// && has higher precendence than ||